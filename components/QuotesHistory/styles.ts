import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  quotation: {
    flexDirection: 'row',
    gap: 20,
    paddingTop: 20,
    paddingBottom: 20,
  },
});

export default styles;
