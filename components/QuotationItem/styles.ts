import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  quotationContent: {
    textAlign: 'center',
  },
  quoationDate: {
    marginTop: 10,
  },
});

export default styles;
